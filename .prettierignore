node_modules
dist
.next
coverage
pnpm-lock.yaml
package-lock.json
*.tsbuildinfo

# Markdown is excluded deliberately, not by oversight.
#
# The ADRs, skills and knowledge base are hand-wrapped prose with deliberate line
# breaks in tables and callouts. Reflowing them adds no consistency that matters and
# destroys structure that does.
#
# Markdown is not unchecked: scripts/validate-knowledge-base.py enforces the parts
# that carry meaning — frontmatter, visibility scoping, locale parity, orphans — and
# the harness validates agent, skill and command frontmatter.
*.md

# Generated by packages/ui-tokens (`tokens:css`) and compared byte-for-byte with what the tokens
# generate. Reformatting it would make that test fail for a reason that is not a token change.
packages/ui-tokens/tokens.css
.playwright-mcp

# Generated, and compared byte for byte by apps/api/src/common/openapi/openapi.spec.ts (T-136).
apps/api/src/metadata.ts

# Generated by drizzle-kit on every `migration:generate`, so reformatting it would come back
# unformatted with the next migration (T-130). The SQL migrations beside it are hand-reviewed.
apps/api/src/database/migrations/meta
